apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: staging
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: myregistry/backend:staging
        ports:
        - containerPort: 8080
        env:
          - name: ENVIRONMENT
            value: "staging"
          - name: CONNECTION_STRING
            value: "Server=database;Port=5432;Database=webstore_staging;User Id=staginguser;Password=stagingpass;"
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
